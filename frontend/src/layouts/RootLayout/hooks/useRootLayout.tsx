import { UseRootLayoutProps } from '../rootLayout.types';

export const useRootLayout = ({ exampleProp }: UseRootLayoutProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
