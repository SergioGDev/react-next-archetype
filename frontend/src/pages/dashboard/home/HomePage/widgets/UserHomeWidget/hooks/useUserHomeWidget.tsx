import { UseUserHomeWidgetProps } from '../userHomeWidget.types';

export const useUserHomeWidget = ({ exampleProp }: UseUserHomeWidgetProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
