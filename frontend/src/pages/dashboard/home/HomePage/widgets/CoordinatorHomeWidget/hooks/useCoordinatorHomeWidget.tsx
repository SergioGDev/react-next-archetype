import { UseCoordinatorHomeWidgetProps } from '../coordinatorHomeWidget.types';

export const useCoordinatorHomeWidget = ({ exampleProp }: UseCoordinatorHomeWidgetProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
