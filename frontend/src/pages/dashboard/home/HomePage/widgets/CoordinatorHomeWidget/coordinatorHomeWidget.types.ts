// Props for the component
export type CoordinatorHomeWidgetProps = {
    // Put here the CoordinatorHomeWidgetProps. For example:
    exampleProp: string;
};

// Props for the hook
export type UseCoordinatorHomeWidgetProp = {
    // Put here the UseCoordinatorHomeWidgetProp. For example:
    exampleProp: string;
};
