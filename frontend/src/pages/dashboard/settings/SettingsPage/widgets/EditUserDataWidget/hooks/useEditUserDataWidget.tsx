import { UseEditUserDataWidgetProps } from '../editUserDataWidget.types';

export const useEditUserDataWidget = ({ exampleProp }: UseEditUserDataWidgetProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
