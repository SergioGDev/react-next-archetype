import { UseLoginPageProps } from '../loginPage.types';

export const useLoginPage = ({ exampleProp }: UseLoginPageProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
