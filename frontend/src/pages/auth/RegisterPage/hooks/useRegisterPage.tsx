import { UseRegisterPageProps } from '../registerPage.types';

export const useRegisterPage = ({ exampleProp }: UseRegisterPageProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
