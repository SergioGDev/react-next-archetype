import { UseDataListContainerProps } from '../dataListContainer.types';

export const useDataListContainer = ({ exampleProp }: UseDataListContainerProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
