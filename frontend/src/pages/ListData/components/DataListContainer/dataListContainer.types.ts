// Props for the component
export type DataListContainerProps = {
    // Put here the DataListContainerProps. For example:
    exampleProp: string;
};

// Props for the hook
export type UseDataListContainerProp = {
    // Put here the UseDataListContainerProp. For example:
    exampleProp: string;
};
