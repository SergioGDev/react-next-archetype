import { UseItemContainerProps } from '../itemContainer.types';

export const useItemContainer = ({ exampleProp }: UseItemContainerProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
