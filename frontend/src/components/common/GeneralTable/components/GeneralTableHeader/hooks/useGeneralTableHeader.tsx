import { UseGeneralTableHeaderProps } from '../generalTableHeader.types';

export const useGeneralTableHeader = ({ exampleProp }: UseGeneralTableHeaderProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
